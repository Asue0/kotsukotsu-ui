import { Box } from "@mui/material";

const DayBox = () => {
  return (
    <Box
      width={150}
      height={150}
      sx={{ border: "1px solid gray", borderRadius: "5px" }}
    >
      메롱
    </Box>
  );
};

export default DayBox;
